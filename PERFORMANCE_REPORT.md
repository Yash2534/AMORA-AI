# Performance Report

- Images use local fallbacks and core image precaching.
- Long screens use scrollable/sliver layouts rather than expanded static trees.
- Reusable motion primitives provide bounded animation durations.
- Controllers in reviewed stateful flows are disposed.

Profile-level performance measurements should be repeated on representative Android and iOS hardware before a production launch.
